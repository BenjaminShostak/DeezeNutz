
const generate = document.getElementById('generate');


const flavors = ["chocolate", "vanilla", "Caramel", "butterscotch", "maple", "white chocolate" ]
const fruit = ["apple", "bananna", "strawberry", "blueberry", "raspberry ", "mango", "peach"]
const  topping = [ "cookie crumb", "chocolate chips","praline", "graham cracker", "coconut"]


function DonutGenerator(){

    for (i = 0 ; i <flavors.length; i++ ){
        console.log(i);
    }
  fruit.forEach(generate => {
      console.log(fruit);
      flavors

      topping.forEach(generate => {
      console.log(topping);
     dAnswer = ("How about " + flavors + fruit + topping);
  });


  DonutGenerator
      // Event setup using a convenience method
$( "result-container" ).click(function DonutGenerator() {
    console.log(dAnswer);
    return dAnswer;
  }


}